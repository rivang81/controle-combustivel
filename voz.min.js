var __defProp = Object.defineProperty;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
const UNIDADES_NUM = {
  zero: 0,
  um: 1,
  uma: 1,
  dois: 2,
  duas: 2,
  tres: 3,
  quatro: 4,
  cinco: 5,
  seis: 6,
  sete: 7,
  oito: 8,
  nove: 9,
  dez: 10,
  onze: 11,
  doze: 12,
  treze: 13,
  catorze: 14,
  quatorze: 14,
  quinze: 15,
  dezesseis: 16,
  dezessete: 17,
  dezoito: 18,
  dezenove: 19
};
const DEZENAS_NUM = {
  vinte: 20,
  trinta: 30,
  quarenta: 40,
  cinquenta: 50,
  sessenta: 60,
  setenta: 70,
  oitenta: 80,
  noventa: 90
};
const CENTENAS_NUM = {
  cem: 100,
  cento: 100,
  duzentos: 200,
  duzentas: 200,
  trezentos: 300,
  trezentas: 300,
  quatrocentos: 400,
  quatrocentas: 400,
  quinhentos: 500,
  quinhentas: 500,
  seiscentos: 600,
  seiscentas: 600,
  setecentos: 700,
  setecentas: 700,
  oitocentos: 800,
  oitocentas: 800,
  novecentos: 900,
  novecentas: 900
};
const PALAVRA_NUM = __spreadValues(__spreadValues(__spreadValues({}, UNIDADES_NUM), DEZENAS_NUM), CENTENAS_NUM);
const KM_UNIDADE = /* @__PURE__ */ new Set([
  "km",
  "kms",
  "quilometro",
  "quilometros",
  "kilometro",
  "kilometros",
  "quilometragem",
  "milha",
  "milhas",
  "mi",
  "mile",
  "miles"
]);
const LITRO_UNIDADE = /* @__PURE__ */ new Set(["litro", "litros", "l", "lts", "galao", "galoes", "gallon", "gallons", "gal"]);
const PRECO_ANTES = /* @__PURE__ */ new Set(["preco", "custou", "custando", "valor", "reais", "real", "price", "cost"]);
const KM_ANTES = /* @__PURE__ */ new Set(["rodei", "rodou", "rodados", "rodado", "andei", "fiz", "drove"]);
const COMB_PALAVRA = {
  gasolina: "gasolina",
  gasoline: "gasolina",
  gas: "gasolina",
  etanol: "etanol",
  alcool: "etanol",
  ethanol: "etanol",
  alcohol: "etanol"
};
function tokenizarDitado(texto) {
  let s = texto.toLowerCase().normalize("NFD").replace(/[̀-ͯ]/g, "");
  s = s.replace(/r\$\s*/g, " reais ");
  s = s.replace(/(\d)[,.](\d)/g, "$1.$2");
  s = s.replace(/(\d)(km|kms|l|lts)\b/g, "$1 $2");
  s = s.replace(/[^a-z0-9. ]+/g, " ");
  return s.split(/\s+/).map((t) => t.replace(/^\.+|\.+$/g, "")).filter(Boolean);
}
function lerNumeroPorExtenso(tokens, i) {
  let total = 0;
  let atual = 0;
  let ultimo = Infinity;
  while (i < tokens.length) {
    const t = tokens[i];
    if (t === "mil" && (atual > 0 || ultimo === Infinity)) {
      total += (atual || 1) * 1e3;
      atual = 0;
      ultimo = 1e3;
      i++;
      continue;
    }
    const v = PALAVRA_NUM[t];
    if (v !== void 0) {
      if (v >= ultimo) break;
      atual += v;
      ultimo = v;
      i++;
      continue;
    }
    if (t === "e" && i + 1 < tokens.length) {
      const prox = PALAVRA_NUM[tokens[i + 1]];
      if (prox !== void 0 && prox < ultimo || tokens[i + 1] === "mil") {
        i++;
        continue;
      }
      break;
    }
    if (t === "virgula") {
      const d = lerParteDecimal(tokens, i + 1);
      if (d) return { valor: total + atual + d.valor, prox: d.prox };
      break;
    }
    break;
  }
  return { valor: total + atual, prox: i };
}
function lerParteDecimal(tokens, i) {
  const t = tokens[i];
  if (t === void 0) return null;
  if (/^\d+$/.test(t)) return { valor: parseFloat("0." + t), prox: i + 1 };
  if (PALAVRA_NUM[t] !== void 0) {
    const r = lerNumeroPorExtenso(tokens, i);
    const escala = r.valor < 10 ? 10 : r.valor < 100 ? 100 : 1e3;
    return { valor: r.valor / escala, prox: r.prox };
  }
  return null;
}
function interpretarDitado(texto, opts = {}) {
  var _a;
  const us = opts.sistema === "us";
  const PRECO_MAX = us ? 60 : 15;
  const DIST_MIN = us ? 12 : 20;
  const DIST_MAX = us ? 6200 : 9999;
  const VOL_MAX = us ? 32 : 120;
  const tokens = tokenizarDitado(texto);
  const itens = [];
  for (let i = 0; i < tokens.length; ) {
    const t = tokens[i];
    if (/^\d+(\.\d+)?$/.test(t)) {
      let v = parseFloat(t);
      i++;
      if (tokens[i] === "virgula") {
        const d = lerParteDecimal(tokens, i + 1);
        if (d) {
          v += d.valor;
          i = d.prox;
        }
      }
      itens.push({ n: v });
    } else if (PALAVRA_NUM[t] !== void 0 || t === "mil") {
      const r = lerNumeroPorExtenso(tokens, i);
      itens.push({ n: r.valor });
      i = r.prox;
    } else {
      itens.push(t);
      i++;
    }
  }
  const res = { combustivel: null, km: null, litros: null, preco: null, precoGasolina: null, precoEtanol: null };
  for (const it of itens) {
    const c = typeof it === "string" ? COMB_PALAVRA[it] : void 0;
    if (c === "gasolina" && res.combustivel === null) res.combustivel = "gasolina";
    if (c === "etanol") res.combustivel = "etanol";
  }
  const numIdx = [];
  itens.forEach((it, i) => {
    if (typeof it === "object") numIdx.push(i);
  });
  const usado = /* @__PURE__ */ new Set();
  const precosPorComb = { gasolina: null, etanol: null };
  for (let i = 0; i < itens.length; i++) {
    const comb = typeof itens[i] === "string" ? COMB_PALAVRA[itens[i]] : void 0;
    if (!comb || precosPorComb[comb] !== null) continue;
    let j = i + 1;
    let saltos = 0;
    let achou = true;
    while (j < itens.length && typeof itens[j] === "string") {
      const w = itens[j];
      if (COMB_PALAVRA[w] || KM_UNIDADE.has(w) || LITRO_UNIDADE.has(w) || ++saltos > 2) {
        achou = false;
        break;
      }
      j++;
    }
    if (!achou || j >= itens.length) continue;
    let v = itens[j].n;
    const consumidos = [j];
    if (Number.isInteger(v) && v < 100) {
      let j2 = j + 1;
      const entre = [];
      while (j2 < itens.length && typeof itens[j2] === "string") {
        entre.push(itens[j2]);
        j2++;
      }
      if (j2 < itens.length) {
        const cent = itens[j2].n;
        const soConectores = entre.length > 0 && entre.every((w) => w === "e" || w === "reais" || w === "real");
        const centavosDepois = itens[j2 + 1] === "centavos" || itens[j2 + 1] === "centavo";
        if (Number.isInteger(cent) && cent < 100 && (centavosDepois || soConectores)) {
          v += cent / 100;
          consumidos.push(j2);
        }
      }
    }
    let k = consumidos[consumidos.length - 1] + 1;
    let rejeita = false;
    for (; k < itens.length && typeof itens[k] === "string"; k++) {
      const w = itens[k];
      if (KM_UNIDADE.has(w)) {
        rejeita = true;
        break;
      }
      if (LITRO_UNIDADE.has(w)) {
        const ant = itens[k - 1];
        if (ant !== "por" && ant !== "o" && ant !== "per" && ant !== "a") rejeita = true;
        break;
      }
    }
    if (rejeita || !(v > 0 && v <= PRECO_MAX)) continue;
    precosPorComb[comb] = v;
    consumidos.forEach((x) => usado.add(x));
  }
  res.precoGasolina = precosPorComb.gasolina;
  res.precoEtanol = precosPorComb.etanol;
  if (res.precoGasolina !== null && res.precoEtanol !== null) {
    res.combustivel = null;
  } else if (res.precoGasolina !== null || res.precoEtanol !== null) {
    res.preco = (_a = res.precoGasolina) != null ? _a : res.precoEtanol;
  }
  for (let k = 0; k < numIdx.length; k++) {
    const i = numIdx[k];
    if (usado.has(i)) continue;
    let v = itens[i].n;
    const fim = k + 1 < numIdx.length ? numIdx[k + 1] : itens.length;
    const depois = itens.slice(i + 1, Math.min(fim, i + 5)).filter((x) => typeof x === "string");
    const iniAntes = k > 0 ? numIdx[k - 1] + 1 : 0;
    const antes = itens.slice(Math.max(iniAntes, i - 3), i).filter((x) => typeof x === "string");
    if (depois.includes("meio") || depois.includes("meia")) v += 0.5;
    let destino = null;
    if (antes.some((w) => PRECO_ANTES.has(w)) || antes[antes.length - 1] === "a") destino = "preco";
    else if (antes.some((w) => KM_ANTES.has(w))) destino = "km";
    else {
      for (let d = 0; d < depois.length && !destino; d++) {
        const w = depois[d];
        if (KM_UNIDADE.has(w)) destino = "km";
        else if (LITRO_UNIDADE.has(w)) {
          destino = ["por", "o", "per"].includes(depois[d - 1]) ? "preco" : "litros";
        } else if (w === "reais" || w === "real") destino = "preco";
      }
    }
    if (!destino || res[destino] !== null) continue;
    if (destino === "preco" && Number.isInteger(v) && v < 100 && k + 1 < numIdx.length) {
      const j = numIdx[k + 1];
      const cent = itens[j].n;
      const entre = itens.slice(i + 1, j).filter((x) => typeof x === "string");
      const soConectores = entre.length > 0 && entre.every((w) => w === "e" || w === "reais" || w === "real");
      const centavosDepois = itens[j + 1] === "centavos" || itens[j + 1] === "centavo";
      if (Number.isInteger(cent) && cent < 100 && (centavosDepois || soConectores)) {
        v += cent / 100;
        usado.add(j);
      }
    }
    res[destino] = v;
    usado.add(i);
  }
  for (const i of numIdx) {
    if (usado.has(i)) continue;
    const v = itens[i].n;
    if (res.km === null && v >= DIST_MIN && v <= DIST_MAX) {
      res.km = v;
      usado.add(i);
    } else if (res.litros === null && v > 0 && v <= VOL_MAX) {
      res.litros = v;
      usado.add(i);
    } else if (res.preco === null && v > 0 && v <= PRECO_MAX) {
      res.preco = v;
      usado.add(i);
    }
  }
  return res;
}
